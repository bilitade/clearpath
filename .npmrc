# Use one pnpm store path so different pnpm installs (nvm vs system) don't fight.
store-dir=~/.local/share/pnpm/store/v10

# Skip the interactive "reinstall from scratch?" prompt in CI/scripts.
# Fix the root cause by always using the pinned pnpm version (see packageManager).
confirm-modules-purge=false
